{
  "name": "web3rsvp",
  "version": "1.0.0",
  "description": "RSVP dapp",
  "main": "index.js",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network mumbai",
    "script": "node scripts/run.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "RSVP",
    "dapp"
  ],
  "author": "Stacey Graham",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}
